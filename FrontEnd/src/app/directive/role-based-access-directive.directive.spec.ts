import { RoleBasedAccessDirectiveDirective } from './role-based-access-directive.directive';

describe('RoleBasedAccessDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleBasedAccessDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
